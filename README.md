# FSD Project

## Structure
- `front-end/` — React/Vite Online Banking Dashboard
- `back-end/` — reserved for the backend (currently empty)

## Setup
Open a terminal in `front-end/` and run:

```bash
npm install
npm run dev
```
